diff --git a/.github/workflows/main.yml b/.github/workflows/main.yml
index 60d2053f..29149359 100644
--- a/.github/workflows/main.yml
+++ b/.github/workflows/main.yml
@@ -63,7 +63,48 @@ jobs:
             echo "Probably the creation of the cache is not yet complete. Will continue working without the cache."
           fi
 
+  build:
+    needs: cache
+    runs-on: ubuntu-latest
+    steps:
+      - uses: actions/checkout@v2
+
+      - name: Login to Docker Hub
+        uses: docker/login-action@v2
+        with:
+          username: ${{ secrets.DOCKERHUB_USERNAME }}
+          username: ${{ secrets.DOCKERHUB_TOKEN }}
+
+      # - name: Set up Docker Buildx
+      #   uses: docker/setup-buildx-action@v2
+
+      # - name: CVAT server. Getting cache from the default branch
+      #   uses: actions/cache@v2
+      #   with:
+      #     path: /tmp/cvat_cache_server
+      #     key: ${{ runner.os }}-build-server-${{ needs.cache.outputs.sha }}
+
+      # - name: CVAT server. Extract metadata (tags, labels) for Docker
+      #   id: meta
+      #   uses: docker/metadata-action@master
+      #   with:
+      #     images: ${{ secrets.DOCKERHUB_USERNAME }}/cvat_server
+
+      # - name: CVAT UI. Getting cache from the default branch
+      #   uses: actions/cache@v2
+      #   with:
+      #     path: /tmp/cvat_cache_ui
+      #     key: ${{ runner.os }}-build-ui-${{ needs.cache.outputs.sha }}
+
+      # - name: CVAT UI. Extract metadata (tags, labels) for Docker
+      #   id: meta
+      #   uses: docker/metadata-action@master
+      #   with:
+      #     images: ${{ secrets.DOCKERHUB_USERNAME }}/cvat_ui
+
+
   Unit_testing:
+      if: github.ref == 'refs/heads/develop'
       needs: cache
       runs-on: ubuntu-latest
       steps:
@@ -139,6 +180,7 @@ jobs:
             ${{ github.workspace }}/lcov.info
 
   E2E_testing:
+    if: github.ref == 'refs/heads/develop'
     needs: cache
     runs-on: ubuntu-latest
     strategy:
